{/* ... inside the main card ... */}
<div className="relative px-8 pb-12 pt-10">
  
  {/* NEW: Header with Action integrated */}
  <div className="mb-12 flex flex-col md:flex-row md:items-start justify-between gap-6 border-b-4 border-black pb-10">
    <div className="space-y-4">
      <div className="flex items-center gap-3">
        <span className="rounded-full bg-black px-4 py-1 text-[10px] font-black tracking-widest text-white uppercase">
          {challenge.category}
        </span>
      </div>
      <h1 className="text-5xl md:text-7xl font-black tracking-tighter italic text-black uppercase leading-[0.8]">
        {challenge.title}
      </h1>
    </div>

    {/* ACTION MOVED HERE: Always visible at the top */}
    <div className="w-full md:w-[300px] shrink-0">
       <JoinChallengeButton
          challengeId={challenge.id}
          isPremium={challenge.isPremium}
          initialHasJoined={hasJoined}
        />
    </div>
  </div>

  {/* NEW: 2-Column Dashboard Grid */}
  <div className="grid grid-cols-1 lg:grid-cols-3 gap-12">
    
    {/* Left Column: Story & Community (Col Span 2) */}
    <div className={cn("lg:col-span-2 space-y-12", showPaywall && "blur-md pointer-events-none")}>
      <section className="space-y-4">
        <h3 className="text-2xl font-black italic uppercase tracking-tighter">The Mission</h3>
        <p className="text-xl font-medium leading-relaxed text-black/70">
          {challenge.description}
        </p>
      </section>

      <section className="space-y-6">
        <h3 className="text-2xl font-black italic uppercase tracking-tighter">Community Intel</h3>
        <div className="grid gap-4 sm:grid-cols-2">
           {/* Reviews here... */}
        </div>
      </section>
    </div>

    {/* Right Column: The "Command Sidebar" (Col Span 1) */}
    <div className={cn("space-y-8", showPaywall && "blur-md pointer-events-none")}>
      
      {/* SEPARATED PROGRESS CARD */}
      <div className="rounded-[35px] border-4 border-black bg-[#A3E635] p-6 shadow-[6px_6px_0px_0px_rgba(0,0,0,1)]">
        <div className="mb-4 flex items-center justify-between font-black uppercase italic tracking-tighter">
          <span>Live Progress</span>
          <span>60%</span>
        </div>
        <div className="h-6 w-full overflow-hidden rounded-full border-4 border-black bg-white/30">
          <div className="h-full w-[60%] bg-black transition-all" />
        </div>
        <p className="mt-3 text-xs font-bold uppercase text-black/60">
          Global completion today
        </p>
      </div>

      {/* QUICK STATS LIST */}
      <div className="space-y-4 rounded-[35px] border-4 border-black bg-zinc-50 p-6">
        <div className="flex items-center justify-between border-b-2 border-black/5 pb-3">
          <span className="text-xs font-black uppercase text-black/40">Duration</span>
          <span className="font-black italic">{challenge.durationDays} DAYS</span>
        </div>
        <div className="flex items-center justify-between">
          <span className="text-xs font-black uppercase text-black/40">Participants</span>
          <span className="font-black italic">{challenge._count?.participations || 0}</span>
        </div>
      </div>

      {/* PARTICIPANT AVATARS MOVED TO SIDEBAR */}
      <div className="px-2">
         <h4 className="mb-4 text-sm font-black uppercase tracking-widest text-black/30">Enrolled</h4>
         <div className="flex flex-wrap gap-2">
            {/* Avatars loop... */}
         </div>
      </div>

    </div>
  </div>
</div>